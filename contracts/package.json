{
  "name": "trustless-verifier",
  "version": "1.0.0",
  "scripts": {
    "deploy": "npx hardhat run deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0"
  }
}